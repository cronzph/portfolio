{
    "name": "portfolio2-root",
    "private": true,
    "scripts": {
        "dev": "cd react-app && npm run dev",
        "build": "cd react-app && npm install && npm run build",
        "preview": "cd react-app && npm run preview"
    }
}